type CardProps = {
    /* Optional content to be displayed inside the card */
    children?: React.ReactNode;
    /* Optional additional CSS classes for the card */
    className?: string;
};

export type { CardProps };
