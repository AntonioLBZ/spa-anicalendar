import { SectionRoot } from './section';
import { SectionTitle } from './section-title';

const Section = {
    Root: SectionRoot,
    Title: SectionTitle,
};

export { Section };
export type { SectionRootProps, SectionTitleProps } from './section.types';
